{
  "name": "@cgames/services",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "npx tsc",
    "type-check": "npm exec tsc -- --noEmit",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cgames/types": "workspace:*",
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.9.0",
    "openai": "^4.103.0"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  }
} 